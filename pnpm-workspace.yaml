packages:
  - 'apps/*'
  - 'packages/*'

# React Native / Metro resolve much more reliably against a flat node_modules.
nodeLinker: hoisted

onlyBuiltDependencies:
  - '@sentry/cli'
  - esbuild
  - sharp
