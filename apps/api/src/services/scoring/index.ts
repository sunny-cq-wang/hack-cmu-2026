/** P3 export surface for P2. TODO(P3): keep this barrel when replacing internals. */
export { recomputeDay } from './recomputeDay';
export { humanScore, petScore, combinedScore, avatarStateFor, moodFor } from './score';
export { streakLength } from './streak';
