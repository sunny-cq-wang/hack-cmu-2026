// PLACEHOLDER — P4 replaces this file (delete it, do not merge into it).
//
// `tasks/P1_MOBILE_CORE.md` §4 says the placeholder renders nothing, so the return
// type is widened to allow null. P4's real button is a floating push-to-talk control
// that opens its own sheet.

export function TalkButton(): React.JSX.Element | null {
  return null;
}
